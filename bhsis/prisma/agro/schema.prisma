// Prisma schema - agro module (placeholder)

generator client {
  provider = "prisma-client-js"
  output   = "../generated/agro"
}

datasource db {
  provider = "postgresql"
  schemas  = ["bhsis"]
}

model ModuleMeta {
  id        String   @id @default(cuid())
  module    String   @default("agro")
  createdAt DateTime @default(now())

  @@schema("bhsis")
}
