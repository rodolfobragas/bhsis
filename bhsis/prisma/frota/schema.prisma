// Prisma schema - frota module (placeholder)

generator client {
  provider = "prisma-client-js"
  output   = "../generated/frota"
}

datasource db {
  provider = "postgresql"
  schemas  = ["bhsis"]
}

model ModuleMeta {
  id        String   @id @default(cuid())
  module    String   @default("frota")
  createdAt DateTime @default(now())

  @@schema("bhsis")
}
