// Prisma schema - clinicas module (placeholder)

generator client {
  provider = "prisma-client-js"
  output   = "../generated/clinicas"
}

datasource db {
  provider = "postgresql"
  schemas  = ["bhsis"]
}

model ModuleMeta {
  id        String   @id @default(cuid())
  module    String   @default("clinicas")
  createdAt DateTime @default(now())

  @@schema("bhsis")
}
