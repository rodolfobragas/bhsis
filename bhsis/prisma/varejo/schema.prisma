// Prisma schema - varejo module (placeholder)

generator client {
  provider = "prisma-client-js"
  output   = "../generated/varejo"
}

datasource db {
  provider = "postgresql"
  schemas  = ["bhsis"]
}

model ModuleMeta {
  id        String   @id @default(cuid())
  module    String   @default("varejo")
  createdAt DateTime @default(now())

  @@schema("bhsis")
}
